{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch local",
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "file": "${workspaceFolder}/index.html"
        }
    ]
}